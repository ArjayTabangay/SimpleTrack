{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiSettings": {
    // Default production-simulation host API URL (override in dev compose)
    "BaseUrl": "http://localhost:51000"
  }
}
